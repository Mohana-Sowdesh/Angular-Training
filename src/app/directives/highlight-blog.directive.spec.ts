import { HighlightBlogDirective } from './highlight-blog.directive';

describe('HighlightBlogDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightBlogDirective();
    expect(directive).toBeTruthy();
  });
});
